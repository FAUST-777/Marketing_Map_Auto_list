# Marketing_Map_Auto_list — 開發日誌與對話過程記錄

**作者：Willie Lin**

---

## 📌 專案現況總覽（濃縮版，2026-07-15 更新）

**目的**：Marketing 業務原本手動翻 Google Maps 抄店家電話再電訪談廣告投放合約 → 改用 Google Places API 一鍵產出名單（CSV + Google Sheet）。

**已完成的名單**（都在 [Google Sheet](https://docs.google.com/spreadsheets/d/1i9PJ-vKfUoblxGPH6yvdpSGZsMg0PRspl0RUnJ2ohGo/edit) 各分頁）：

| 名單 | 筆數 | 有電話 |
|------|------|--------|
| 板橋 牙醫診所（首測） | 20 | 20 |
| 板橋+中和 食物類 | 1,095 | 1,039 |
| 土城 食物類 | 455 | 424 |
| 新莊、新店 食物類（7/6，兩分頁） | 1,118 | 1,052 |
| 全台早午餐/早餐/輕食等 6 分頁（7/8） | ~6,500 | — |
| 中和永和 食物類（7/13） | 960 | 893 |
| 宜蘭全縣 餐廳甜點咖啡（7/14） | 882 | 818 |
| 台中區A 餐廳類・市區8區（7/14） | 1,204 | 1,089 |

**進行中：台中全區三輪計畫**（關鍵字：餐廳、甜點店、咖啡廳、烘焙店）：
- **A（完成）**：中、東、西、南、北、西屯、南屯、北屯
- **B（待跑，含東區補抓）**：東區補抓 + 太平、大里、豐原、烏日、潭子、大雅、沙鹿（≈96 次）
- **C（待跑）**：清水、梧棲、大甲、霧峰、后里、神岡、大肚、龍井、東勢、新社、石岡、大安、外埔、和平（≈90-110 次，超額部分隔天補）

**擱置**：三峽、桃園食物類（使用者指示保留配額，之後指定縣市再跑）。

**過程中的關鍵問題與使用者決定**：

1. **儲存方式** → 使用者決定：只存 GitHub 雲端，本機不留正式副本（要改就 clone）
2. **Repo 權限** → 公開；因此 API Key/服務帳戶金鑰走 `.env` + `service-account.json`（皆 gitignore）
3. **看不懂 Google Cloud Console** → 改用 gcloud CLI 全指令代辦（使用者只做一次 `gcloud auth login`），API 啟用/金鑰/服務帳戶全部指令完成
4. **Sheet 看起來空白** → 資料其實在底部的新分頁，不是預設「工作表1」
5. **簡體地址 bug** → Google 部分店家回傳「板桥区」，加簡轉繁對照表修正
6. **撞到每日配額（429）** → 給使用者兩個選項：綁信用卡解除限制 vs 等每日重置；**使用者先選綁卡，後改為等配額**，剩餘區域分兩天跑

---

## 第 1 輪開發（2026-07-03）｜需求討論 & 專案初建

### 對話過程記錄

**需求提出：**

> 我現在有一個新的專案要處理，請幫我新增到 GitHub 上去。老樣子，要有需求起因跟對話過程記錄。專案名稱叫做 Marketing_Map_Auto_list。

**儲存方式討論：**

> 專案資料想放 GitHub 雲端，本機端應該沒有一定要儲存對吧？

結論：GitHub 即為完整雲端儲存，本機只是工作時的暫存副本。push 上去後本機可刪除，之後要修改再 `git clone` 下來即可。因此本專案不在本機保留正式副本。

**詳細需求說明：**

> 我有一個 Marketing 業務，她都從 Google Map 去找店家資料，再打電話去聯絡，談協助廣告投放的合約。我希望她做得更有效率，所以想確認 Google Map 是不是有 API 可以拉出即時店家資料，就不用翻地圖？

### 需求分析結論

1. **Google Maps 有官方 API** — Places API (New) 的 Text Search，輸入「區域 + 行業」即可回傳店家的名稱、地址、電話、網站、評分、評論數
2. **費用** — 每月有免費額度；電話/網站屬於較高計價的 Contact Data 層級，但業務名單用量（月數千筆內）通常免費額度就夠
3. **合規** — Google 條款規定資料快取不得超過 30 天，本工具定位為「產生當期開發名單」而非永久資料庫，符合條款
4. **產出形式** — CSV（UTF-8 BOM），業務用 Excel 雙擊開啟即可照著名單打電話

### 完成項目

- `search.js` — 主程式：Text Search + 自動翻頁（最多 60 筆）+ CSV 輸出
- `.env.example` — API Key 設定範本（真正的 `.env` 已加入 `.gitignore`）
- `README.md` — 需求起因、API Key 申請步驟、使用方式、費用說明
- 建立 GitHub 公開 repo 並 push

### 技術決策

| 決策 | 原因 |
|------|------|
| Node.js 內建 fetch、零相依套件 | 業務電腦只要裝 Node.js 18+ 就能跑，不用 npm install |
| Places API (New) 而非舊版 | 舊版 Places API 已停止對新專案開放，新版用 FieldMask 精準控制計費 |
| CSV 加 UTF-8 BOM | Excel 直接開啟中文才不會亂碼 |
| API Key 走 `.env` | repo 是公開的，金鑰絕不能 commit |

---

## 第 2 輪開發（2026-07-03）｜需求追加：地理分類

### 對話過程記錄

> 喔對我還有一個需求，需要用「新北/板橋區」這樣去分類，我要把店家區分地理縣市。

### 實作方式

- FieldMask 加入 `places.addressComponents`，從 API 的結構化地址元件直接取出縣市（`administrative_area_level_1`）與行政區（`administrative_area_level_2` / `locality`）
- 元件拆不到時，退回用正規表達式解析 `formattedAddress`（`XX市/縣` + `XX區/鄉/鎮/市`）
- CSV 最前面新增「縣市」「行政區」兩欄，Excel 可直接篩選或做樞紐分析
- 輸出前依「縣市 → 行政區」排序，同區店家排在一起，方便業務分區開發

---

## 第 3 輪開發（2026-07-03）｜需求追加：商業類型分類

### 對話過程記錄

> 還有我需要增加一個欄位，幫我分類店家商業類型，例如：食物/醫院/XX 等等的。原本有這類分類嗎？

答：Google 原本就有——每家店都帶 `primaryType`（如 restaurant、dentist），且 `primaryTypeDisplayName` 可直接回傳中文名稱（如「牙醫診所」）。

### 實作方式

- FieldMask 加入 `primaryType`、`primaryTypeDisplayName`、`types`
- 新增兩欄：
  - **商業大類** — 用規則表把 Google 細分類歸納成 13 個中文大類（餐飲、醫療保健、美容美體、運動健身、教育、住宿、汽機車、金融、不動產、法律、零售、旅遊娛樂、宗教），對不上的歸「其他」
  - **商業類型** — Google 原始細分類的中文顯示名，保留細節（火鍋店 vs 咖啡廳都屬餐飲大類）

---

## 第 4 輪開發（2026-07-03）｜需求追加：自動寫入 Google Sheet

### 對話過程記錄

> 好那現在我開一個 Google Sheet，妳把資料倒入進去（提供了 Sheet 網址）。

現況：機器上沒有現成 Google 憑證（無 gcloud、無服務帳戶金鑰），Places API Key 也尚未申請，因此先把功能完整做好，等憑證備齊即可執行。

### 實作方式

- 新增 `sheets.js`：零相依的 Google Sheets 寫入模組
  - 用 Node 內建 `crypto` 對服務帳戶金鑰簽 RS256 JWT，換取 access token（不裝 googleapis 套件）
  - 分頁不存在自動建立；已存在則清空重寫
- `search.js` 加 `--sheet` 參數：搜尋完除了輸出 CSV，同步寫入 `.env` 的 `GOOGLE_SHEET_ID`，分頁名 = 關鍵字 + 日期
- `.gitignore` 加入 `service-account.json`（repo 公開，金鑰不上傳）

### 待使用者完成（一次性設定）

1. Google Cloud Console 建專案、啟用 **Places API (New)** + **Google Sheets API**
2. 建 API Key → 填入 `.env` 的 `GOOGLE_MAPS_API_KEY`
3. 建服務帳戶 + JSON 金鑰 → 存成 `service-account.json`
4. 把目標 Sheet 分享給服務帳戶 email（編輯者）

---

## 第 5 輪開發（2026-07-03）｜Google Cloud 申請自動化 & 首次實測成功

### 對話過程記錄

> 妳幫我去申請，Google 網頁我有點看不懂。

使用者已自行建立 Cloud 專案（aerobic-form-433316-h6）但看不懂 Console 網頁。解法：安裝 gcloud CLI（winget），使用者只做一次 `gcloud auth login`，其餘申請全用指令完成。

### 完成項目（全程指令，未碰網頁）

1. winget 安裝 Google Cloud SDK（第一次 msstore 來源憑證錯誤，改指定 `--source winget` 成功）
2. `gcloud services enable` 啟用 Places API (New) + Google Sheets API
3. 建立服務帳戶 `marketing-map-bot@aerobic-form-433316-h6.iam.gserviceaccount.com` + JSON 金鑰
4. `gcloud services api-keys create` 建立 API Key（已限制只能呼叫 Places API）
5. 實測「板橋 牙醫診所」：20 筆全有電話，並成功寫入使用者的 Google Sheet

### 遇到的問題與解決

**問題：部分店家的行政區欄位是空的**
Google 對部分店家回傳簡體字地址（「板桥区」），原本的正規表達式只認繁體「區」。
解法：加入簡轉繁字元對照表（台灣地名常見字），addressComponents 與地址字串解析前先轉繁體。修正後 20/20 筆行政區全部正確。

**備註：** 專案尚未綁定計費帳戶，但 Places API 實測可用（免費額度內）。若未來出現 `BILLING_DISABLED` 類錯誤，需到 Cloud Console 綁定計費帳戶。

---

## 第 6 輪開發（2026-07-03）｜批次模式：抓整區的食物類店家

### 對話過程記錄

> 我現在需要幫我抓餐廳、甜點等等食物相關，並且要板橋/中和全部。

（使用者另回報 Sheet 看起來空白——查證後資料其實在「板橋 牙醫診所 2026-07-03」分頁，使用者停在預設的空白「工作表1」。）

### 實作方式

單次 Text Search 上限 60 筆，抓不了「整區全部」。新增批次模式：

- 重構：共用邏輯抽到 `lib.js`（搜尋、地區解析、分類、CSV），`search.js` 變薄殼
- 新增 `batch.js`：`--areas` × `--keywords`（或 `--preset food` 內建 12 個食物關鍵字）交叉搜尋
  - 以 googleMapsUri 去重（同一家店會被多個關鍵字搜到）
  - 地區過濾：Google 會混入鄰近區結果（永和、新莊），只保留目標區
  - 排序改為 縣市 → 行政區 → 商業類型

### 實測結果（板橋+中和 × 12 食物關鍵字，24 組查詢）

抓取 1,417 筆 → 去重 1,238 筆 → 地區過濾 1,095 筆（板橋 615、中和 471、少數永和/新莊為地址含關鍵地名者），其中 1,039 筆有電話。已寫入 Google Sheet 分頁「板橋中和 食物類 2026-07-03」。

---

## 第 7 輪（2026-07-03）｜土城/新店/三峽/桃園 → 撞到每日配額

### 對話過程記錄

> 接下來幫我抓土城/新店/三峽/桃園，一樣是餐廳、甜點、食物相關。

### 結果

- **土城完成**：455 家（424 家有電話），寫入分頁「土城新店三峽桃園 食物類 2026-07-03」
- **新店/三峽/桃園失敗**：API 429 — 未綁計費帳戶的專案 Text Search 配額只有 **100 次/天**，當天已被板橋+中和批次（~72 次）與土城（~34 次）用完

### 後續計畫

使用者選擇不綁卡、等每日配額（太平洋時間午夜 = 台灣約下午 3 點重置）。剩餘工作分兩批：
1. 新店 + 三峽（約 70 次呼叫）
2. 桃園（約 35 次呼叫）

跑完需把結果補進同一個 Sheet 分頁。若之後改綁計費帳戶即可解除限制（免費額度內仍為 $0）。

---

## 第 8 輪（2026-07-06）｜新莊+新店 食物類 + 分頁拆分

### 對話過程記錄

> 還記得我之前用GOOGLE MAP 跑的 各個餐飲店 甜點資料嗎? …我要跑 新莊+新店的 餐飲店家

> 在google sheet 裡面把 新莊 跟新店 分開sheet

### 結果

- 新莊+新店 × food preset（24 組查詢，~72 次呼叫）：1,439 筆 → 去重 1,146 → 地區過濾 1,118（1,052 家有電話），分頁「新莊新店 食物類 2026-07-06」
- 一次性 split.js 依「行政區」欄拆出分頁「新莊 食物類」558 家、「新店 食物類」560 家

---

## 第 9 輪（2026-07-08）｜全台早午餐/早餐/輕食 + 修復「臺/台」過濾誤刪 bug

### 對話過程記錄

> 幫我繼續搜尋 早午餐店 不限縣市 全台皆可 早餐店 早午餐 輕食都包含 在幫我分類 謝謝

第一次以「縣市」為地區單位跑批次（之前都是「區」）。

### 發現的 bug：台北/台中整批被地區過濾誤刪

第一批（北中部 10 縣市 × 早餐店/早午餐/輕食）跑完：1,427 筆去重後過濾只剩 1,136，且台北市僅 13 筆、台中市 6 筆（其他縣市皆 120~160 筆）。

原因：Google 官方地址寫「**臺**北市」「**臺**中市」，而過濾 regex 用搜尋關鍵字「台北」「台中」比對行政區與地址欄，臺≠台 → 台北/台中店家幾乎全數被當成「鄰近地區混入」刪掉。另發現少數地址元件是簡體「桃园市」未轉換。

### 修法

- `lib.js`：S2T 對照表補「园→園、臺→台」等字 → 縣市/行政區欄輸出一律正規化為「台」
- `batch.js`：地區過濾 regex 中「台/臺」互通（`台北` → `[台臺]北`），並加入「縣市」欄一併比對

### 配額策略

全台 21 縣市（跳過連江）× 3 關鍵字 ≈ 189 次呼叫，分兩個配額窗口：
1. 北中部 10 縣市（~90 次）
2. 15:00 配額重置後：台北/台中重跑 + 南部東部離島 9 縣市（33 組 ≈ 99 次，壓線）

跑完合併去重、依商業類型分類寫入分頁。

---

## 第 10 輪（2026-07-13）｜中和+永和 食物類 + 本機環境重建

### 對話過程記錄

> 總之我現在需求就是"中和+永和的美食/小吃/甜點" 我去google sheet看 的確沒有這個 妳也幫我檢查一下 沒有就直接幫我優先抓這個

先核對 Sheet 實際分頁確認確實沒有中和+永和（記憶記錄與 Sheet 有落差，以 Sheet 為準），隨後執行。

### 結果

`node batch.js --areas "中和,永和" --preset food --sheet` → **960 家（893 家有電話）**，分頁「中和永和 食物類 2026-07-13」。

### 環境重建

本輪起 repo 在本機保留正式副本（`C:\Users\USER\Marketing_Map_Auto_list`）：`.env` 以 `gcloud services api-keys get-key-string` 取回 API Key 重建、服務帳戶金鑰重新簽發（皆在 .gitignore，不進 repo）。後續多日批次（台中三輪）需要常駐環境，故不再用完即丟。

---

## 第 11 輪（2026-07-14）｜宜蘭全縣 + 路名撞區名 bug 修正

### 對話過程記錄

> 幫我找找 宜蘭 所有 餐廳 甜點 咖啡店 一樣寫入sheet 謝謝

縣級任務首次採「鄉鎮拆分」跑法：宜蘭、羅東、礁溪、頭城、蘇澳、冬山、員山、五結 8 鄉鎮 × 3 關鍵字（餐廳、甜點店、咖啡廳），24 組 ≈ 72 次呼叫。

> 我看宜蘭抓完之後 最後有7筆是中和區? 怎麼會這樣? 哪邊出問題?

### Bug：搜尋區名撞上外縣市路名

搜「員山」（宜蘭員山鄉）時，Google 連同**中和區員山路**上的店一起回傳（店名多帶「員山」，如 CAMA CAFE 中和員山店）。原過濾邏輯「地址包含區名即放行」，「新北市中和區**員山**路」包含「員山」→ 誤放行 7 筆。

### 修法

`batch.js` 過濾改為**優先比對行政區欄位**（中和區 ≠ 員山 → 正確擋掉），行政區解析不出來時才退回比對完整地址。

### 結果

清理後 **882 家（818 家有電話）**，分頁「宜蘭全縣 餐廳甜點咖啡 2026-07-14」。

---

## 第 12 輪（2026-07-14~15）｜台中全區三輪計畫 + 通用區名污染 bug（--prefix）

### 對話過程記錄

> 我是想跑完台中全區 那就分兩次 那幫我sheet 命名 "台中區A" 隔天"台中區B" 後面一樣幫我壓日期+餐廳類

台中 29 區 × 4 關鍵字（餐廳、甜點店、咖啡廳、烘焙店）估 240~280 次呼叫，免費配額下實際需**三輪**（非兩輪）：A 市區 8 區 / B 外圍大區 / C 山海線與鄉區。

### A 輪執行 → 發現通用區名污染 bug

A 輪（中、東、西、南、北、西屯、南屯、北屯）跑完 1,256 筆，但混入 **52 筆外縣市**：「東區、北區」這類通用區名直接當查詢詞，Google 回傳了台南東區（43 筆）、新竹北區（2 筆）的店，且行政區欄位同名（都叫「東區」）可通過過濾。另有 5 筆縣市欄解析異常（「台灣/台灣省」，查郵遞區號 402~408 實為台中店家，修正保留）。

### 修法

`batch.js` 新增 **`--prefix` 參數**：
1. 查詢詞加縣市前綴（「台中東區 餐廳」），從源頭減少外縣市結果
2. 過濾加一道縣市檢查：縣市欄或地址必須包含前綴，同名行政區（台南東區）直接擋掉

**教訓**：跑直轄市/省轄市的「東西南北中」通用區名，一律要帶 `--prefix 縣市名`。

### 結果與後續

- 台中區A 清理後 **1,204 家（1,089 家有電話）**，分頁「台中區A 餐廳類 2026-07-14」
- 東區因污染損失約 43 筆有效結果 → **B 輪補抓東區**
- B 輪（今日 15:00 後）：東區補抓 + 太平、大里、豐原、烏日、潭子、大雅、沙鹿，`--prefix 台中`，≈96 次
- C 輪（隔日 15:00 後）：清水、梧棲、大甲、霧峰、后里、神岡、大肚、龍井、東勢、新社、石岡、大安、外埔、和平，≈90-110 次
